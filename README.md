# 🐉 Fantasy Creature Generator API

**A RESTful API built with TypeScript, Express, and Zod** that procedurally generates fantasy creatures inspired by _Lord of the Rings_, _Final Fantasy_ worlds.  
Each creature is unique — with random **names**, **titles**, **elements**, **rarities**, **traits**, and **stats**.

---

## ✨ Features

- ⚙️ Procedural creature generation
- 🔮 Element & rarity system (_common → legendary_)
- 💪 Dynamic stats scaled by rarity & element
- 👥 Team generation with optional distinct members
- ✅ Type-safe validation using Zod
- 🧪 Fully tested (smoke, unit, and integration tests)

---

## 🧠 How It Works

- Rarity defines the creature’s base stat budget
  (e.g., common = 300, epic = 520, legendary = 600).

- Elements (fire, water, shadow, etc.) add unique stat bonuses.

- Names are generated by combining fantasy-inspired syllables for variety.

- Traits and titles give each creature unique flavor and personality.

- /team repeats generation for multiple creatures, with optional distinct filtering.

## 🚀 API Endpoints

### 🧭 `GET /`

Returns API information and available routes.

**Example Response**

```json
{
  "name": "Fantasy Creature Generator API",
  "routes": [
    { "path": "/creatures/random", "query": ["element", "rarity", "minTotal", "maxTotal"] },
    { "path": "/creatures/team", "query": ["n", "distinct", "element", "rarity", "minTotal", "maxTotal"] }
  ]
}
```
---

### 🐲 `GET /creatures/random`

Generates a single random fantasy creature.

#### 🔧 Query Parameters

| Parameter | Type | Description | Example |
|------------|------|-------------|----------|
| `element` | string | Force specific element | `element=fire` |
| `rarity` | string | Force creature rarity | `rarity=epic` |
| `minTotal` | number | Minimum total stat points | `minTotal=500` |
| `maxTotal` | number | Maximum total stat points | `maxTotal=600` |

**Example Request**

GET http://localhost:5003/creatures/random?element=fire&rarity=epic

**Example Response**
```json
{
  "name": "Aethsoulrend",
  "title": "the Frostbound Herald",
  "elements": ["fire", "air"],
  "rarity": "epic",
  "stats": {
    "hp": 88,
    "attack": 120,
    "defense": 95,
    "speed": 80,
    "magic": 75,
    "luck": 62
  },
  "traits": [
    "smells faintly of burnt roses and ozone",
    "bleeds silver embers",
    "heart is bound in runic chains"
  ]
}
```
---

### 🧙‍♂️ `GET /creatures/team`

Generates a team of multiple randomly generated creatures.

#### 🔧 Query Parameters

| Parameter | Type | Description | Example |
|------------|------|-------------|----------|
| `n` | number | Number of creatures to generate (1–12) | `n=6` |
| `distinct` | boolean | Ensures all creatures have unique name/title combos | `distinct=true` |
| `element` | string | Force all creatures to share a specific element | `element=shadow` |
| `rarity` | string | Force a specific rarity for all creatures | `rarity=rare` |
| `minTotal` | number | Minimum total stat points per creature | `minTotal=400` |
| `maxTotal` | number | Maximum total stat points per creature | `maxTotal=550` |

---

**Example Request**

GET http://localhost:5003/creatures/team?n=5&distinct=true


---

## 🧪 Testing

Includes:
- ✅ Smoke tests for generator and endpoints
- 🧩 Comprehensive unit test for generation logic
- 🌐 Integration tests for API routes

**Run tests**
```bash
npm test
```
## ⚙️ Running Locally

**1️⃣ Install dependencies**
```bash
npm install
```
**2️⃣ Start the server**

```bash
npm run dev
```
**3️⃣ Open in browser**

http://localhost:5003/

**📁 Project Structure**

```bash
src/
 ├── index.ts              # Entry point (Express setup)
 ├── routes/creatures.ts   # API routes (/random, /team)
 ├── services/generator.ts # Core creature generation logic
 ├── lib/types.ts          # Type definitions
 └── middleware/error.ts   # Error handling

```

